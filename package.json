{
  "name": "agentpact",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "compile": "turbo run compile",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
